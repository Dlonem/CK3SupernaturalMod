namespace = spn_versioninfo
#Main Info, Change global var name to get it to prompt during yearly pulse
spn_versioninfo.0001 = {
	type = character_event
	title = spn_versioninfo.0001.t

	desc = spn_versioninfo.0001.desc
	theme = spn_info

	trigger = {
		NOT = { exists = global_var:version_1_04 }
	}

	immediate = {
		set_global_variable = {
			name = version_1_04
			value = yes
		}
	}

	option = {
		name = spn_versioninfo.0001.a
		custom_tooltip = spn_versioninfo.0001.a.tt
	}
}