# Can this character go on raids?
# root: the potential raider
can_raid = {
	OR = {
		can_raid_trigger = yes
		AND = {
			is_landed = yes
			any_directly_owned_province = {
				has_building_with_flag = wc_evil_fortress_flag
			}
		}
	}
}