# Supernatural Hunt -- the tracking phase's weekly pool (2.35). Fired from supernatural_hunt.txt on_weekly_pulse
# for the host of a Monster Hunt. Each event carries its own trigger; the pool just weights them.
spn_hunt_default_event_selection = {
	random_events = {
		chance_of_no_event = { value = 30 }
		30 = spn_hunt.0101	# The Survivor
		30 = spn_hunt.0102	# What the Graves Say
		15 = spn_hunt.0103	# The Wrong Man
		25 = spn_hunt.0104	# Lessons in the Dark
		25 = spn_hunt.0105	# A Fellow Hunter's Tale
		40 = spn_hunt.0106	# A Word with [name]
		# 2.35 -- only fires in a county that really holds more than one of them (its own trigger).
		35 = spn_hunt.0112	# You Are Being Counted
		# 2.35 -- only fires where a real local hunter exists to meet.
		30 = spn_hunt.0113	# Another Man's Trail
		# 2.35b -- the field events (10_spn_hunt_field_events.txt). The first three carry their own KIND gate
		# and only one of them can ever be valid on a given hunt, which is the point: a hybrid hunt and a
		# wolf hunt should not read the same.
		45 = spn_hunt.0150	# Two Different Animals  (hybrid only)
		40 = spn_hunt.0151	# By the Moon            (werewolf only)
		40 = spn_hunt.0152	# Not a Drop             (vampire only)
		35 = spn_hunt.0153	# They Are All Very Sure (compelled county only)
		30 = spn_hunt.0154	# What the Dogs Will Not Pass
		25 = spn_hunt.0156	# The Ones Who Helped
		# 2.35 PART TWO -- the demon field (10_spn_demon_hunt_events.txt). Both carry their own gate: the
		# first only on a demon trail, the second wherever the county holds somebody who signed something.
		45 = spn_hunt.0160	# Nothing Was Taken       (demon only)
		30 = spn_hunt.0161	# The Ones Who Signed     (a soulless courtier in the county)
		# 2.35 PART TWO -- and the witch trail's own week (10_spn_witch_trail_events.txt). This pool serves
		# a Witch Hunt ridden to a real trail as well as a Monster Hunt, so it is where this belongs; its
		# trigger needs var:spn_hunt_witch_trail and a culprit whose house is a coven.
		45 = spn_hunt.0170	# They Are Not Working Alone
	}
}

# 2.35 -- the Witch kind's own week (10_spn_witch_hunt_events.txt). Deliberately thinner than the
# monster pool and with a high chance of nothing: the Witch Hunt is three weeks of asking around, and
# a hunt that produces one good lead and one hard choice is the right amount of hunt.
spn_hunt_witch_event_selection = {
	random_events = {
		chance_of_no_event = { value = 45 }
		40 = spn_hunt.0120	# A List of Names
		30 = spn_hunt.0121	# The Woman They Talk About
	}
}
