#Check if the user had a trait added from the ruler designer on game start
spn_ruler_designer_check = {
	effect = {
		every_player = {
			trigger_event = spn_init.0005 #Init Vampire from RD
			trigger_event = spn_init.1005 #Init Lycan from RD
			trigger_event = spn_init.2005 #Init Hybrid from RD
			trigger_event = spn_init.3005 #Init Hunter from RD
		}
	}
}