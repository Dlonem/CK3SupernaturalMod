###########################
# Supernatural Hunt intents -- the Witch Hunt kind (2.35, D10 -- thin)
###########################

# Host. Shown only when the Witch Hunt kind is picked; runs the existing Witchcraft chain inside the activity.
spn_root_out_witchcraft_intent = {
	icon = pious_intent

	is_shown = {
		scope:special_option ?= flag:spn_hunt_kind_witch
	}
	ai_will_do = {
		value = 50
		if = { limit = { has_trait = zealous }   add = 20 }
		if = { limit = { has_trait = paranoid }  add = 10 }
	}
	auto_complete = yes
	on_invalidated = { trigger_event = activity_system.0090 }
	scripted_animation = { animation = personality_zealous }
}
