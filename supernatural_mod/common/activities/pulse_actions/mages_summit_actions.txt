mca_gain_sc_pulse = {
	icon = learning
	is_valid = {
		is_current_phase_active = yes
		any_attending_character = {
			is_true_mage = yes
			is_legendary_mage_trigger = no
		}
	}

	weight = {
		value = 1
	}

	effect = {			
		random_attending_character = {
			limit = {
				is_true_mage = yes
				is_legendary_mage_trigger = no
			}
			save_scope_as = atte
		}
		
		add_activity_log_entry = {
			key = mca_gain_sc_pulse
			tags = { pulse_action }
			character = scope:atte
			
			scope:atte = {
				gain_sc_xp1_effect = yes
			}
		}
	}
}