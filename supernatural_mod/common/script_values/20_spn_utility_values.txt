#Calculates the total skill values of a character
spn_total_skill = {
	value = diplomacy
	add = martial
	add = stewardship
	add = intrigue
	add = learning
}

#Calc chance for immortal Pregnancy
spn_supernatural_pregnancy_chance = {
	value = fertility
	multiply = 30 #Changed for SPN 2.01 for mirroring, originally 40, then 25, now its 30
}

spn_human_pregnancy_chance = {
	value = fertility
	multiply = 50
}

#Calculate pregnancy chance for Supernaturals
spn_pregnancy_chance = {
	value = 0

	if = { #Mother is Supernatural
		limit = { 
			spn_has_supernatural_trait = yes
		}
		add = spn_supernatural_pregnancy_chance
	}
	else = {
		add = spn_human_pregnancy_chance
	}

	if = { #Father is Supernatural
		limit = { 
			scope:real_father = { spn_has_supernatural_trait = yes }
		}
		scope:real_father = {
			add = spn_supernatural_pregnancy_chance
		}
	}
	else = {
		scope:real_father = {
			add = spn_human_pregnancy_chance
		}
	}

	if = { #If Mother has had Supernatural children already
		limit = {
			has_character_flag = spn_has_supernatural_children
			spn_has_supernatural_trait = yes
		}
		#-5 per supernatural child already born. The multiply used to sit here at the if-block
		#level, which multiplied the ENTIRE running total: after one child a couple computed
		#(80 + 1) x -5 = -405, clamped to a flat 5%, and every later pregnancy was silently
		#ended at reveal. Nesting it inside the add is what was meant.
		add = {
			value = var:spn_number_of_supernatural_children
			multiply = -5
		}
	}

	if = { #If father has had Supernatural children already
		limit = {
			scope:real_father = { 
				has_character_flag = spn_has_supernatural_children 
				spn_has_supernatural_trait = yes
			}
		}
		scope:real_father = {
			add = {
				value = var:spn_number_of_supernatural_children
				multiply = -5
			}
		}
	}
	
	if = { #If Mother and/or Father are Vampire
		limit = {
			OR = {
				scope:real_father = { has_trait = supernatural_vampire }
				scope:mother = { has_trait = supernatural_vampire }
			}
		}
		multiply = 0			
	}
		
	if = { #Mother and father are not the same type of Supernatural
		limit = {
			OR = {
				AND = {
					has_trait = supernatural_hybrid
					scope:real_father = { has_trait = supernatural_werewolf }
				}
				AND = {
					has_trait = supernatural_werewolf
					scope:real_father = { has_trait = supernatural_hybrid }
				}
			}
		}
		multiply = 0.25 #Changed for SPN 2.01 for mirroring, originally 20
	}
	else_if = {
		limit = {
			OR = {
				AND = {
					has_trait = supernatural_hybrid
					scope:real_father = {
						has_trait = supernatural_werewolf
					}
				}
				AND = {
					OR = {
						has_trait = supernatural_werewolf
					}
					scope:real_father = { has_trait = supernatural_hybrid }
				}
			}
		}
		multiply = 0.5
	}
	min = 5 #Originally 0, but trying to boost Hybrid babies
	max = 75
}

#Calculates how many years the character has been immortal
spn_years_as_immortal = {
    value = 0 #Changed from just value = age in v2.08
    add = age
    if = {
        limit = { exists = var:spn_age_aquired_immortality }
        subtract = var:spn_age_aquired_immortality
    }
}

spn_calc_gradual_visual_age = {
	value = 0
	add = age

	if = {
		limit = { exists = var:spn_age_aquired_immortality  var:spn_age_aquired_immortality > 16 }
		subtract = var:spn_age_aquired_immortality
	}
	else = {
		subtract = 16
	}

	divide = 5
	
	if = {
		limit = { var:spn_age_aquired_immortality > 16 }
		add = var:spn_age_aquired_immortality
	}
	else = {
		add = 16
	}
}

sire_vs_progeny_margin_accept = {
    # STEP per rung (tune by changing all add = ±2 if you want a different weight)
    value = 0

    ########################
    # ACTOR power rungs (+)
    ########################
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 5  }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 5  } } } } add = 2 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 10 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 10 } } } } add = 2 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 15 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 15 } } } } add = 2 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 20 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 20 } } } } add = 2 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 25 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 25 } } } } add = 3 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 30 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 30 } } } } add = 3 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 35 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 35 } } } } add = 3 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 40 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 40 } } } } add = 3 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 45 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 45 } } } } add = 4 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 50 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 50 } } } } add = 4 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 55 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 55 } } } } add = 4 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 60 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 60 } } } } add = 4 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 65 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 65 } } } } add = 5 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 70 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 70 } } } } add = 5 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 75 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 75 } } } } add = 5 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 80 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 80 } } } } add = 5 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 85 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 85 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 90 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 90 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 95 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 95 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 100 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 100 } } } } add = 6 }

    ############################
    # RECIPIENT power rungs (−)
    ############################
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 5  }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 5  } } } } add = -2 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 10 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 10 } } } } add = -2 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 15 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 15 } } } } add = -2 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 20 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 20 } } } } add = -3 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 25 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 25 } } } } add = -2 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 30 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 30 } } } } add = -3 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 35 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 35 } } } } add = -3 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 40 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 40 } } } } add = -3 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 45 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 45 } } } } add = -3 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 50 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 50 } } } } add = -4 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 55 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 55 } } } } add = -4 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 60 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 60 } } } } add = -4 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 65 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 65 } } } } add = -4 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 70 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 70 } } } } add = -4 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 75 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 75 } } } } add = -5 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 80 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 80 } } } } add = -5 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 85 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 85 } } } } add = -5 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 90 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 90 } } } } add = -5 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 95 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 95 } } } } add = -5 }
    if = { limit = { scope:recipient = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 100 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 100 } } } } add = -5 }
}

spn_compulsion_actor_power = {
    value = 0

    # Small base if you have any vampiric power at all
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value >= 0 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value >= 0 } } } } add = 8 }

    # Smoother, stronger rungs
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 5  }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 5  } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 10 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 10 } } } } add = 8 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 20 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 20 } } } } add = 10 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 30 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 30 } } } } add = 10 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 40 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 40 } } } } add = 10 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 50 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 50 } } } } add = 10 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 60 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 60 } } } } add = 8 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 70 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 70 } } } } add = 8 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 80 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 80 } } } } add = 10 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 90 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 90 } } } } add = 10 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 100 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 100 } } } } add = 8 }
}

spn_compulsion_actor_power_50 = {
    value = 0
    # Half of each rung from spn_compulsion_actor_power (rounded)
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value >= 0 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value >= 0 } } } } add = 5 }

    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 5  }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 5  } } } } add = 4 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 10 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 10 } } } } add = 5 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 20 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 20 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 30 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 30 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 40 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 40 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 50 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 50 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 60 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 60 } } } } add = 5 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 70 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 70 } } } } add = 5 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 80 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 80 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 90 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 90 } } } } add = 6 }
    if = { limit = { scope:actor = { OR = {
        has_trait_xp = { trait = supernatural_vampire track = vampirism        value > 100 }
        has_trait_xp = { trait = supernatural_hybrid  track = hybrid_vampirism value > 100 } } } } add = 5 }
}


spn_compulsion_target_resist = {
    value = 0
    # Learning as willpower
    if = { limit = { scope:recipient = { learning > 8  } } add = -2 }
    if = { limit = { scope:recipient = { learning > 12 } } add = -3 }
    if = { limit = { scope:recipient = { learning > 16 } } add = -4 }
    if = { limit = { scope:recipient = { learning > 20 } } add = -5 }

    # Werewolf harder to compel
    if = { limit = { scope:recipient = { has_trait = supernatural_werewolf } } add = -20 }

    # Personality weights
    if = { limit = { scope:recipient = { has_trait = brave    } } add = -25 }
    if = { limit = { scope:recipient = { has_trait = stubborn } } add = -25 }
    if = { limit = { scope:recipient = { has_trait = content  } } add = -10 }

    if = { limit = { scope:recipient = { has_trait = craven   } } add = 20 }
    if = { limit = { scope:recipient = { has_trait = trusting } } add = 20 }
    if = { limit = { scope:recipient = { has_trait = fickle   } } add = 15 }

    # Your call: zealous is easier to sway
    if = { limit = { scope:recipient = { has_trait = zealous  } } add = 10 }
}


spn_compulsion_margin_accept = {
    value = 0

    # Base actor power
    add = { value = spn_compulsion_actor_power }

    # Perk: add half again (1.5× total)
    if = {
        limit = {
			scope:actor = {
				OR = {
					has_perk = compulsion_perk		#Vampire
					has_perk = undead_command_perk 	#Hybrid
				}
			} 
		}
        add = { value = spn_compulsion_actor_power_50 }
    }

    # Target resistance (unchanged)
    add = { value = spn_compulsion_target_resist }
}

# Supernatural Hunter resistance to Compulsion
# Baseline −10 at 0 XP, then −10 per 10 XP up to −100 at 90+ XP.
spn_compulsion_hunter_resist = {
    value = 0

    # Only applies to Hunters
    if = { limit = { scope:recipient = { has_trait = supernatural_hunter } } add = -20 }

    # Scale by Monster-Hunting track XP (cap at 90)
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 9 } } } add = -10 }
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 19 } } } add = -10 }
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 29 } } } add = -10 }
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 39 } } } add = -10 }
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 49 } } } add = -10 }
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 59 } } } add = -10 }
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 69 } } } add = -10 }
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 79 } } } add = -10 }
    if = { limit = { scope:recipient = {
        has_trait_xp = { trait = supernatural_hunter track = monster_hunting value > 89 } } } add = -10 }
}

#Chance (0-100) that a child of a Demon parent is born demon-blooded (gains the demon0 trait).
#Used at birth in spn_on_genetic_birth. ROOT is the newborn child; scope:mother / scope:real_father are the parents.
spn_demon_birth_chance = {
	value = 0

	if = { #Both parents are Demons -> stronger blood
		limit = {
			scope:mother = { is_a_demon_trigger = yes }
			scope:real_father = { is_a_demon_trigger = yes }
		}
		add = 75
	}
	else = { #One Demon parent, one Human -> even odds
		add = 50
	}

	if = { #A Greater Demon (demon2) parent passes the blood more reliably
		limit = {
			OR = {
				scope:mother = { has_trait = demon2 }
				scope:real_father = { has_trait = demon2 }
			}
		}
		add = 15
	}

	min = 0
	max = 100
}


# Phase 2: SOLO chance to feed cleanly (power-forward + forgiving). Allies & county control are added at the roll. Clamped 5-95.
# Consolidated, diminishing-returns power bonus (caps at +20) - shown as ONE 'Vampiric Power' line.
vf_power_bonus = {
	value = 0
	# Even +4 per tier, caps at +20 (no diminishing - power should always feel rewarding).
	if = { limit = { activity_host = { OR = { has_trait_xp = { trait = supernatural_vampire track = vampirism value > 10 } has_trait_xp = { trait = supernatural_hybrid track = hybrid_vampirism value > 10 } } } } add = 4 }
	if = { limit = { activity_host = { OR = { has_trait_xp = { trait = supernatural_vampire track = vampirism value > 25 } has_trait_xp = { trait = supernatural_hybrid track = hybrid_vampirism value > 25 } } } } add = 4 }
	if = { limit = { activity_host = { OR = { has_trait_xp = { trait = supernatural_vampire track = vampirism value > 40 } has_trait_xp = { trait = supernatural_hybrid track = hybrid_vampirism value > 40 } } } } add = 4 }
	if = { limit = { activity_host = { OR = { has_trait_xp = { trait = supernatural_vampire track = vampirism value > 60 } has_trait_xp = { trait = supernatural_hybrid track = hybrid_vampirism value > 60 } } } } add = 4 }
	if = { limit = { activity_host = { OR = { has_trait_xp = { trait = supernatural_vampire track = vampirism value > 80 } has_trait_xp = { trait = supernatural_hybrid track = hybrid_vampirism value > 80 } } } } add = 4 }
}

# Consolidated, diminishing-returns ally bonus (caps at +15) - shown as ONE 'Allies' line.
vf_allies_bonus = {
	value = 0
	if = { limit = { any_attending_character = { count >= 2 OR = { has_trait = supernatural_vampire has_trait = supernatural_hybrid } } } add = 5 }
	if = { limit = { any_attending_character = { count >= 3 OR = { has_trait = supernatural_vampire has_trait = supernatural_hybrid } } } add = 4 }
	if = { limit = { any_attending_character = { count >= 4 OR = { has_trait = supernatural_vampire has_trait = supernatural_hybrid } } } add = 3 }
	if = { limit = { any_attending_character = { count >= 5 OR = { has_trait = supernatural_vampire has_trait = supernatural_hybrid } } } add = 2 }
	if = { limit = { any_attending_character = { count >= 6 OR = { has_trait = supernatural_vampire has_trait = supernatural_hybrid } } } add = 1 }
}
vf_feeding_success_chance = {
	value = 50

	# Evaluated with the ACTIVITY as root (on_start + GUI breakdown): use activity_host / activity_location / any_attending_character.

	add = { value = vf_power_bonus desc = vf_sc_power }
	if = { limit = { activity_location = { exists = county } } activity_location.county = { add = { value = spn_county_prosperity_value multiply = -0.2 desc = vf_sc_development } } }
	if = { limit = { activity_location = { exists = county } } add = { value = activity_location.county.county_control multiply = -0.15 desc = vf_sc_control } }
	if = { limit = { activity_location = { county ?= { any_county_province_epidemic = { count >= 1 outbreak_intensity < apocalyptic } } } } add = { value = 12 desc = vf_sc_plague } }
	if = { limit = { activity_location = { county ?= { holder ?= { is_at_war = yes } } } } add = { value = 8 desc = vf_sc_war } }
	if = { limit = { activity_host = { has_trait = supernatural_hybrid } } add = { value = 5 desc = vf_sc_hybrid } }
	if = { limit = { activity_host = { has_trait = spn_known_vampire } } add = { value = -15 desc = vf_sc_known_vampire } }
	else_if = { limit = { activity_host = { has_trait = spn_known_hybrid } } add = { value = -15 desc = vf_sc_known_hybrid } }
	if = { limit = { activity_host = { has_character_flag = vf_laid_trap } } add = { value = 20 desc = vf_sc_prep } }
	else_if = { limit = { activity_host = { has_character_flag = vf_scouted_location } } add = { value = 10 desc = vf_sc_prep } }
	if = { limit = { activity_host = { has_st_satiate_thirst_intent_trigger = yes } } add = { value = 5 desc = vf_sc_intent } }
	else_if = { limit = { activity_host = { has_st_embrace_the_night_intent_trigger = yes } } add = { value = -10 desc = vf_sc_intent } }
	else_if = { limit = { activity_host = { has_st_strengthen_the_blood_intent_trigger = yes } } add = { value = -5 desc = vf_sc_intent } }
	add = { value = vf_allies_bonus desc = vf_sc_allies }
	if = { limit = { exists = activity_host } add = { value = activity_host.prowess multiply = 0.15 desc = vf_sc_prowess } }
	if = { limit = { exists = activity_host } add = { value = activity_host.learning multiply = 0.1 desc = vf_sc_learning } }
	if = { limit = { activity_host = { has_trait = shrewd } } add = { value = 5 desc = vf_sc_shrewd } }
	if = { limit = { exists = activity_host } add = { value = activity_host.intrigue multiply = 0.1 desc = vf_sc_intrigue } }
	# Traits that help or hurt a discreet feed.
	if = { limit = { activity_host = { OR = { has_trait = beauty_good_1 has_trait = beauty_good_2 has_trait = beauty_good_3 } } } add = { value = 5 desc = vf_sc_comely } }
	if = { limit = { activity_host = { has_trait = reckless } } add = { value = -5 desc = vf_sc_reckless } }
	# Easter egg: an Original at the table - uses the mod's existing is_mikaelson_trigger (dynasty 5000001).
	if = { limit = { any_attending_character = { is_mikaelson_trigger = yes } } add = { value = 15 desc = vf_sc_mikaelson } }
	# 2.32 -- EVENTS. Everything the player actually chose during the run. This is the term that
	# was missing, and its absence is why the bar and its own tooltip could never agree: the bar
	# read a variable snapshotted at on_start, the tooltip re-evaluated this value live, and the
	# fourteen in-run choices existed only in the variable. Vanilla's hunt bar solves it exactly
	# here -- 04_ep2_hunt_values.txt:815 adds `var:hunt_success_chance_event` the same way.
	if = {
		limit = { exists = var:vf_feeding_chance_event }
		add = { value = var:vf_feeding_chance_event desc = vf_sc_events }
	}
	min = 5
	max = 95
}

# 2.32 -- How far is this city from where the host actually lives?
# root = the province being considered. scope:host = the would-be feeder.
#
# province_filter on the feeding activity is `all` now, so the planner offers the whole
# world; this value is what puts "nearby" back. Anchored on the capital for a landed ruler
# and on the domicile for a camp, because a landless adventurer has no capital_province and
# would otherwise evaluate against nothing.
#
# Vanilla ships the same idiom in monument_expedition and 00_activity_values.txt:2170.
vf_host_distance_to_province = {
	if = {
		limit = { scope:host = { exists = capital_province } }
		value = "scope:host.capital_province.squared_distance(root)"
	}
	else_if = {
		limit = { scope:host = { exists = domicile } }
		value = "scope:host.domicile.domicile_location.squared_distance(root)"
	}
	else = {
		value = "scope:host.location.squared_distance(root)"
	}
}

# 2.32 -- what a capped creature track converts into.
# Calibrated against the game folder, not guessed: vanilla is 1,000 XP per perk
# (00_lifestyle_values.txt -- massive_lifestyle_xp = 500, "Half of xp needed for 1 perk point"),
# and the workhorse grant inside vanilla ACTIVITIES is medium_lifestyle_xp = 100. The creature
# trees cost 1,500 a perk (vampire and hybrid), so the honest equivalent is that constant x1.5.
# Written against the constant rather than as a bare 150 so it tracks if Paradox retunes it.
#
# NOTE: this is deliberately NOT calibrated against the mod's four hand-typed lifestyle grants
# (ev1.09 {100 400} learning, ev1.20 {100 300} and ev1.26 {50 150} intrigue). Those sit on
# VANILLA trees at 1,000/perk -- a different currency, and not a precedent for this one.
spn_creature_lifestyle_xp_converted = {
	value = medium_lifestyle_xp
	multiply = 1.5
}

# ============================================================================
# 2.32 -- THE PROSPERITY WRAPPERS.
# Paradox intends to replace `development` with prosperity, and something further after that.
# Every read of development_level in Supernatural now goes through this value and every write
# through spn_county_prosperity_change_effect, so the eventual swap is two definitions rather
# than a search-and-replace across events, script values and activity tags.
#
# Evaluated in COUNTY scope. Script values cannot be dereferenced through a scope path in CK3
# (no vanilla precedent for it), so callers enter the county scope first -- which is the same
# idiom vanilla's hunt_success_chance_value uses with activity_host.
# ============================================================================
spn_county_prosperity_value = {
	value = development_level
}

# Convenience wrapper for the two gold-scaling sites, which need the activity's county and sit
# inside an effect rather than a scope block.
spn_activity_county_prosperity_value = {
	value = 0
	if = {
		limit = { exists = scope:activity  scope:activity.activity_location = { exists = county } }
		scope:activity.activity_location.county = { add = spn_county_prosperity_value }
	}
}

# 2.32 -- how far a vampire will travel to feed, as a SQUARED map-pixel distance.
#
# Pulled out into its own value so the compatibility patch can override it for AGOT's map without
# touching this file. `squared_distance_large` is vanilla's own "roughly one France away" (650 px),
# which on the base map is about 11.5 provinces of reach.
#
# AGOT's map is the same 9216 px wide but 6144 tall with far fewer, larger provinces -- measured at
# 78 px across against vanilla's 57, i.e. 1.37x wider -- so the same radius there reaches only about
# half as many counties. The patch raises this to squared_distance_huge for that reason.
spn_vf_max_feeding_distance = {
	value = squared_distance_large
}

# 2.33 -- "would this blow have killed me?", as a PERCENTAGE, mirroring vanilla knight_killed.
#
# WHY THIS EXISTS. spn_blood_took_the_blow wins essentially every combat pulse for a blooded
# character (base 100000, the same idiom AGOT uses for knight_dragon_joined_battle), because a
# weighted pool cannot be made to intercept another entry -- adding weight only DILUTES the pool.
# So the entry takes the character out of the lottery and then re-rolls the death itself, here.
#
# The three factors are copied from knight_killed's own chance block (00_knight_phase_events.txt:971):
#   base 30, scaled by (30 - prowess)/30 floored at 0.1 ("Everyone dies"), scaled again by
#   outnumbering, and zeroed when vastly outnumbering.
# The final divide converts a POOL WEIGHT into a PERCENTAGE: knight_none alone is base 2000, and
# the rest of the table adds ~250, so a weight of W was worth about W/2250 of the picks.
# 22.5 = 2250/100. Approximate on purpose -- knight_none carries modifiers that can raise it to
# ~4700 for a decorated player, which makes this slightly generous rather than slightly harsh.
# THIS IS THE TUNING KNOB for how often the blood is called upon.
spn_blood_blow_chance = {
	value = 30
	multiply = {
		value = 30
		subtract = prowess
		divide = 30
		min = 0.1
	}
	if = {
		limit = {
			exists = scope:combat_side
			scope:combat_side.side_strength > scope:combat_side.enemy_side.side_strength
		}
		multiply = {
			value = scope:combat_side.enemy_side.side_strength
			divide = scope:combat_side.side_strength
			multiply = 1.4
			max = 1
		}
	}
	if = {
		limit = {
			exists = scope:combat_side
			scope:combat_side.side_strength > scope:combat_side.enemy_side.vastly_outnumbered_combat_side_threshold
		}
		multiply = 0
	}
	divide = 22.5
}
